Put the service account key of your Firebase project here, as the file
fcm-service-account.json. It is needed for push notifications only; without it
the server works and notifications arrive while the phone holds a connection.
See the "Push notifications" section of the README.
